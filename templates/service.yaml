apiVersion: v1
kind: Service
metadata:
  name: {{ include "capstone-chart-vulnerable-php83.fullname" . }}
  labels:
    {{- include "capstone-chart-vulnerable-php83.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "capstone-chart-vulnerable-php83.selectorLabels" . | nindent 4 }}
